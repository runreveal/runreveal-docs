# MongoDB

The mongodb source polls your MongoDB atlas account every minute for new access events.

Once the source is added RunReveal will populate new logs from MongoDB atlas every minute.

<img src="/mongo-1.png"/>